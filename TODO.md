# TODO

## ⚠️ Before merging `fairycore`

- [ ] Remove the temporary daytime mock in `src/context/WeatherContext.tsx`
      (search for `TEMP MOCK`) — it forces `is_day = 1` for design preview.

## Higher-effort design ideas (reserved for later)

- [ ] Corner flourish / vine SVG accents on cards
- [ ] Whimsical active-tab indicator (sparkle/leaf marker instead of flat highlight)
- [ ] Hover "bloom" animation on buttons/links

## Low-effort ideas (not yet started)

- [ ] Gradient text on Hero temperature display
- [ ] Custom scrollbar styling
- [ ] Softer/muted error `Alert` color (currently default Chakra red)
- [ ] Soft `drop-shadow` glow filter on weather condition icon

## Notes

- Consider adding Fairycore (Dynamic), Fairycore (Day), Fairycore (Night) themes
